{% extends 'front/base.html.twig' %}

{% block title %}{{ 'topic.new_title'|trans }}{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb" class="mt-3">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('app_home') }}">{{ 'common.home'|trans }}</a></li>
            <li class="breadcrumb-item active">{{ 'topic.new_heading'|trans }}</li>
        </ol>
    </nav>

    <h1 class="mb-4">{{ 'topic.new_heading'|trans }}</h1>

    {{ form_start(topicForm) }}
        {{ form_row(topicForm.title) }}
        {{ form_row(topicForm.category) }}
        {{ form_row(topicForm.content) }}
        {{ form_row(topicForm.picture) }}
        {{ form_widget(topicForm.submit) }}
    {{ form_end(topicForm) }}
{% endblock %}

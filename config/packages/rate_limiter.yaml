framework:
    rate_limiter:
        registration:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        api:
            policy: 'sliding_window'
            limit: 30
            interval: '1 minute'
